"""
core/shared — Platform-agnostic modules.
These files are identical on Windows and Linux.
Import via: from core.shared import agent, memory, vocab, guardrails
"""
from core.shared import agent, memory, vocab, guardrails
